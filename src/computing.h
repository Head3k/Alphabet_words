#ifndef COMPUTING_H
#define COMPUTING_H
#include <stdio.h>

int sort(char* k1, char* k2);
int stringing(int count_words);
char computing(char array[][16],int count_words);
void print();
int stringing(int count_words);
#endif
